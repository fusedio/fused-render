__pycache__/
*.pyc
.venv/
venv/
# pip-managed project (pip install -e ".[dev]"); a uv.lock is only ever a
# stray artifact of running uv here, never committed.
uv.lock
build/
dist/
*.egg-info/
.DS_Store
scripts/vendor-codemirror/node_modules/
scripts/vendor-sci/node_modules/
scripts/vendor-three/node_modules/
.claude/worktrees/
frontend/node_modules/
fused_render/static/shell-dist/
# Starter-kit authoring skills are build-time copies of the canonical skills/
# (D106, same pattern as shell-dist); never committed, shipped via `artifacts`.
fused_render/template_starter/.claude/skills/
uv.lock
fused_render/_baked_branch.py
.coverage
.coverage.*
coverage.xml
htmlcov/
# Local agent skills dir (per-checkout tooling, not part of the repo).
.agents/
# Legacy Rust supervisor build cache, superseded by the Python supervisor.
windows/supervisor/target/
# C launcher build byproduct (scripts/build_windows_installer.ps1).
*.obj
