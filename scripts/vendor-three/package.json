{
  "name": "fused-render-three-vendor",
  "private": true,
  "description": "Build workspace that bundles Three.js (core + OrbitControls + GLTFLoader) into a self-contained ESM bundle for fused-render's read-only glb viewer template. Not shipped; only the built bundle is committed. See README.md.",
  "version": "0.0.0",
  "scripts": {
    "build": "./build.sh"
  },
  "dependencies": {
    "esbuild": "^0.25.11",
    "three": "0.160.0"
  }
}
