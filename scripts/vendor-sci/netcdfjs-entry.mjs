// Bundled into fused_render/templates/vendor/netcdfjs.bundle.mjs. netcdf_template.html
// does `import { NetCDFReader } from "…"` — that is the only symbol it uses.
export { NetCDFReader } from "netcdfjs";
